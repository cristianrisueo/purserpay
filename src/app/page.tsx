import Landing from "@/views/Landing"

// Landing ('/') — a Server Component. Its interactive children (HeroPayoutCard, the
// FAQ accordion) carry their own "use client" boundaries.
export default function Page() {
  return <Landing />
}
