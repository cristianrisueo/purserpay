// Tailwind v4 for Next.js uses the PostCSS plugin (the Vite build used
// @tailwindcss/vite). The CSS-first config lives in src/styles/globals.css.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
}

export default config
