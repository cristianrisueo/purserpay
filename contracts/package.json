{
  "name": "purser-pay-contracts",
  "private": true,
  "version": "0.0.1",
  "description": "PurseDisperseUsdt — the ownerless, immutable USDT-TRC20 batch-disperse contract for Purser Pay. Deliberately isolated from the root Vite/React package.json: this is a wholly separate Solidity/Hardhat toolchain.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.1.0"
  }
}
