/design-reference
sprint_report.txt
SPRINTS.md
/screenshots
/mock_data

# Local measurement artifacts (regenerated by `node measure.js`).
measurements.json

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# Foundry (contracts/) — generated / re-derivable output, not wanted on GitHub.
# Only the source (src/, test/, foundry.toml) is tracked; everything below is
# rebuilt by `forge build` / `forge install`.
# (.gitignore has no inline comments — keep notes on their own lines.)
/contracts/out/
/contracts/cache/
/contracts/lib/
/contracts/broadcast/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env
.env
.env.*
# ...but keep the tracked example (documents the required vars, no real values)
!.env.local.example

# Vercel
.vercel

# Coverage
coverage
